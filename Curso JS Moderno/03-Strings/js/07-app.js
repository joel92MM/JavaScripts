const producto ="Monitor de 20 pulgadas";
/* pasar a mayusculas */
console.log(producto.toUpperCase);
/* pasar a minusculas */
console.log(producto.toLocaleLowerCase);

const precio=399;
/* pasa el tipo numero a string */
console.log(precio.toString());
